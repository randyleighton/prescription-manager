- content_for(:title, "Drug List")
.jumbotron
  %h1 Drug List
  %p Create a library of drugs to use in your prescriptions.
  = link_to "Add a New Drug", new_drug_path, :class =>"btn btn-default btn-lg"

%div.col-xs-12
  - @drugs.each do |drug|
    .drug_tag= link_to "#{drug.name}", drug_path(drug)
